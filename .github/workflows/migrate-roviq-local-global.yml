name: Obsolete ROVIQ Local migration — disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Stop obsolete cross-repository migration
        run: |
          echo "This workflow is intentionally disabled."
          echo "ROVIQ Local database migrations are owned by the roviq-local repository and must target ROVIQ Local2 there."
          exit 1
